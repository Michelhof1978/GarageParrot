import React from "react";


const Logoblc = () => (
  
      <div className="">
        <img className="img-fluid " src={props.imageUrl} alt={props.altText} />
      </div>
    
);

export default Logoblc;
